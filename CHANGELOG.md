## 0.1.24

- 修复安装版导出完整剪映草稿时偶发 ENOENT/opendir 的问题，模板文件会从 app.asar 安全复制到可写缓存。
- 修复图片生成失败后重新生成按钮可能无法再次点击的问题。
- 优化剪映草稿导出稳定性，降低历史审核页、素材目录和模板路径导致的导出失败。
- 更新 GitHub README、在线更新说明和发布说明，确保版本号与中文内容可读。

## 0.1.23

- 优化图片/视频素材提示词：改为更具体的中文电影分镜描述，强调人物、地点、动作、时间、镜头和情绪，减少抽象提示导致画面同质化。
- 优化参考图匹配：按故事、人物/场景类型和当前分镜动态选择参考图，不再把所有资产图都传给每个素材。
- 优化 Agnes 视频生成：视频素材优先使用可用公网参考图，本地参考图不可用时给出降级说明并使用中文分镜生成。
- 优化剪映字幕导出：导出前可调用 AI 生成断句点，服务端二次拆分长字幕并去除字幕标点，降低长字幕和重叠风险。
- 修复服务端测试导出入口缺少 replaceTextContent 导致历史审核/剪映导出相关功能异常的问题。

## 0.1.22

- 修复剪映字幕断句过长的问题：新增短句阈值和服务端二次拆分，避免多句合成一条长字幕。
- 修复 AI 分析一直显示正在分析的问题：审核页和历史审核页都增加 120 秒超时保护。
- 增加 LLM 供应商请求超时保护，模型长时间无响应时会返回可理解错误，不再卡住界面。
- 补充回归测试，覆盖长字幕拆分、AI 分析超时和旧审核页兼容补丁。

# Jaygo Cut Changelog

## 0.1.21

- ??????????????????????????? capcut-cli ????????? 7 ???????????????
- ?? AI ????? chunk ?????????????????????????????????????????????????
- ?????????????????????????????????????????????
- ???????????????????????AI/??????????????????????????
- ?????????????????????????????????????

## 0.1.20

- AI 分析新增本地剪辑知识库：应用启动时扫描历史审核草稿，学习用户常删口癖、弱重复、跑题段和常见文字纠错，不重复学习已处理草稿。
- 原文校对改为安全关键词纠错：只允许同等字数的短词替换，降低“词变句”“句变词”和误改其它句子的风险。
- 插入图片/视频素材提示词升级为短中文分镜，要求具体人物、地点、动作、镜头和情绪，减少直接复制文案、抽象约束和同质化画面。
- Agnes 视频生成按免费额度调整为 1 分钟 1 次请求；失败或未完成素材不会进入剪映草稿导出，减少请求超时和 Media Not Found。
- 发布建议继续强化爆款标题结构；主设置保存提示、审核页字幕标点乱码规则同步修复。

## 0.1.19

- 剪映草稿字幕导出改为按句子断句，逗号也算一句；导出字幕文本会去除标点符号，更贴合剪映二次编辑习惯。
- 主设置页保存设置后增加成功/失败提示，保存动作更明确。
- 发布建议新增默认“自媒体爆款”风格，生成 10 个不同句式的视频标题、5 个封面标题、简介和话题参考。
- “关键词”改为“话题参考”，统一以 `#话题` 展示，方便自媒体平台发布时选题和挂话题。
- 插入素材的图片配图上传替换会按当前配图比例自动重采样，降低本地图片尺寸不匹配导致替换失败或导出异常的概率。

## 0.1.18

- 修复旧审核页导出剪映草稿时报 `buildTimeMapper is not defined` 的问题，服务端会给历史审核页自动注入兼容脚本。
- 修复图片/视频素材生成中断后重新打开仍停留在“生成中”的问题，异常状态会恢复为可重试。
- 视频素材数量支持自动匹配；Agnes 图片与视频请求共用 RPM20 限流，降低触发接口限制的概率。
- 增强 Agnes 视频轮询与任务 ID 兼容，优先使用官方 `/agnesapi?video_id=...` 查询，并识别 `remixed_from_video_id`。
- “LLM 对话”升级为“AI 管家”，支持通过对话执行导出剪映、重新生成图片/视频、生成配图/视频素材和调用 AI 分析。
- “LLM 标记”更名为“AI 分析”，补强口播剪辑提示词，强调先理解结构、保护主线、保守删除。

## 0.1.17

- 修复审核页“导出剪映”点击后缺少即时反馈的问题，导出前会刷新剪映目录和模板候选，失败时给出明确弹窗提示。
- 插入素材的人物/场景资产图支持预览和删除，便于用户管理参考资产。
- 正式图片和视频生成改为按分镜内容自动匹配参考资产，不再把所有资产图都传给每个镜头，降低画面同质化。
- 修复 Agnes 视频素材生成把本地参考图误传给视频接口导致 `Invalid image` 或 `fetch failed` 的问题；视频接口只使用公网参考图，失败时自动降级为文本生成。
- 增强 Agnes 视频创建和轮询的网络重试，减少偶发连接失败对生成流程的影响。
- 清理审核页工具区空白占位，折叠工具面板更紧凑，给审核文本留出更多空间。

## 0.1.16

- 完整剪映草稿导出支持自动识别剪映/CapCut 草稿目录，导出后默认直接写入草稿根目录，用户打开剪映即可看到新项目。
- 支持填写或选择已有剪映模板草稿路径，导出时基于模板生成一个新的 Jaygo Cut 草稿，不覆盖用户原模板。
- 审核页剪映导出面板新增导出模式、草稿根目录识别按钮和模板草稿候选，减少手动复制草稿文件夹的步骤。
- 服务端新增 `/api/jianying-draft-targets`，用于扫描默认剪映/CapCut 草稿目录并列出可用模板草稿。
- 扩充回归测试，覆盖自定义剪映草稿根目录导出、自动注册草稿目录和审核页新导出控件。

## 0.1.15

- 插入素材悬浮面板增加整体滚动条，生成图片和视频素材较多时不会遮挡视频素材区域。
- 图片素材新增动效选择：无动效、缓慢推进、缓慢拉远、上下左右平移。
- 裁剪合成阶段支持按所选图片动效写入最终成片，视频素材保持原样铺满以避免二次运动。
- 强化媒体素材合成链路测试，覆盖审核页动效入口、服务端参数清洗和 FFmpeg 动态滤镜。
## 0.1.14

- 审核页新增“预听当前删除点”按钮，支持 `S` 快捷键，可播放删除片段前后约 2 秒，并高亮当前预听片段。
- 新增“删除诊断”面板，统计删除片段总数、删除总时长、最长/最短片段、连续密集删除段和风险片段。
- 风险片段支持点击跳转，便于快速检查过短、过长、紧贴保留词等容易残音或吞字的位置。
- 执行裁剪前强制保存当前审核状态，并生成 `review-state.backup.json`，降低裁剪失败或异常关闭导致丢失数据的风险。
- 新增剪辑精度模式：`保守`、`标准`、`干净`。默认仍为 `标准`，只在提交裁剪任务时调整删除边界，不改变用户看到的文本时间戳。
- 裁剪失败弹窗改为更易理解的原因提示，详细错误保留在日志中，并增加“复制诊断信息”能力。
- 历史记录增强：检测原视频是否缺失，支持重新定位视频；视频缺失时仍可打开只读审核页查看文本和状态。
- 扩充回归测试，覆盖删除诊断、剪辑模式边界、裁剪前备份、历史记录健康状态和审核页新增入口。

## 0.1.13

- 主界面设置移除“图片比例”，避免和审核页的视频配图比例重复；图片比例现在统一在审核页选择。
- 保留审核页图片比例传参，生成图片时仍按审核页当前选择生效。
- 包含此前审核页快捷键指南、文本搜索替换、剪映草稿导出、LLM/视频配图等修复。

## 0.1.12

- 审核页新增快捷键指南按钮，支持 `Space` 播放/暂停、`Ctrl+Z` 多步撤回、`Ctrl+Y`/`Shift+Ctrl+Z` 重做、`Ctrl+F` 搜索纠错、`Ctrl+S` 保存草稿。
- 审核页新增文本搜索与批量替换，可用于纠正“他/她/它”和专有名词识别错误。
- 补全剪映草稿导出，生成草稿目录，并支持内置字幕样式和用户自定义字幕模板。
- 修复审核页左右悬浮按钮在非全屏或窄窗口下重叠的问题。
- 优化 LLM 口播剪辑提示词，降低误删有效内容的概率。
- 在线更新模块新增“本次更新内容”展示，支持从服务器 `release-notes.json` 读取说明。

## 0.1.10 - 0.1.11

- 修复审核页空白、历史审核恢复、波形缩放和播放光标跳动问题。
- 优化静音阈值、撤销、拖选和边界补偿，减少剪辑后残留音。
- 增加 SRT/TXT 字幕导出和视频配图实验功能。

## 0.1.8 - 0.1.9

- 将运行时输出目录改为英文，降低 Windows 中文路径编码风险。
- 增加阿里 Qwen3-ASR 异步识别和本地模型检测/安装流程。
- 增加在线更新基础能力。
